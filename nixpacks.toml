[phases.setup]
# 使用 nodejs_23 以满足 Vite 7.2.4 对 >22.12.0 的要求
pkgs = ["nodejs_23", "npm"]

[phases.install]
# 正常安装依赖
cmds = ["npm ci", "npm install -g edgeone"]

[phases.build]
# 执行构建
cmds = ["npm run build"]

[start]
cmd = "node deploy-wrapper.cjs"